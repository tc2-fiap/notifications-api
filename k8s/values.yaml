replicaCount: 1

image:
  repository: ghcr.io/tc2-fiap/notifications-api
  tag: latest
  pullPolicy: Always

service:
  port: 8080

postgres:
  host: postgres
  port: "5432"
  database: fiap_games
  username: notifications_role
  searchPath: notifications

email:
  provider: console
